import React from 'react';

const Report = () => {
    return (
        <div>
            <h2>Report Page</h2>
            <p>This is the report page. we will enable user to select an inclusive date range.</p>
            <p>we will display all matching logs, majority mood over that date range, number of logs made, longest daily streak, and an average of all colors.</p>
        </div>
    );
};

export default Report;